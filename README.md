# Voice to Text 

This is a python script to convert voice to text in python.

Contains a modular function for usage.

## Install Requirements

```bash
pip install -r requirements.txt
```

## Execution
for cli
```bash
python main.py
```
for other modules
define a class and then the corresponding function

